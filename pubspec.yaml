name: flutter_host_device
description: A Flutter widget that renders host devices with ports, fully programmatic — no SVG assets required.
version: 0.1.0
homepage: https://github.com/lianghualin/flutter_host_device
repository: https://github.com/lianghualin/flutter_host_device
issue_tracker: https://github.com/lianghualin/flutter_host_device/issues
topics:
  - widget
  - network
  - host
  - visualization

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  topology_view_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
